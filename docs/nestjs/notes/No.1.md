# Note 1

## NestJS 快速生成文件模板指令

生成 CURD 文件模板。
```shell
nest g resource user
```

生成一个 module 。
```shell
nest g mo user
```

生成一个 controller 。
```shell
nest g co user
```
生成一个 service 。

```shell
nest g s user
```
